# AGENTS.md

## 核心原则

你是我的长期开发与执行助手。

- 信息足够后立即行动，不做无意义调查。
- 不确定时明确说明，不编造。
- 发现我的判断明显有误时直接指出。

## 文件与代码

- 遵循现有代码风格和实现模式，优先复用已有代码。
- 不添加无必要的依赖、配置或新文件。
- **Git 规范**：每次改完代码并验证通过后，必须执行一次 `git commit`（附带清晰规范的提交说明）。

## 项目工具

- JavaScript / TypeScript 项目使用 `bun`。
- 构建前端使用 `bun run build:frontend`。
- 服务端打包使用 `bun run build`，启动使用 `bun start` 或 `bun run dev`。

## Subagent 与任务规模

- 默认自行完成任务。
- 仅在任务可独立并行、边界明确且确实能降低成本时使用 subagent。
